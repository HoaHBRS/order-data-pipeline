name: Terraform CD

on:
  workflow_dispatch:

concurrency:
  group: terraform-dev
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  plan:
    if: github.ref == 'refs/heads/main'
    name: Plan dev infrastructure
    runs-on: ubuntu-latest
    timeout-minutes: 10

    defaults:
      run:
        working-directory: infra/terraform

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.15.8"

      - name: Log in to Azure using OIDC
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_PLAN_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_PLAN_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_PLAN_SUBSCRIPTION_ID }}

      - name: Verify Azure identity
        run: |
          az account show \
            --query "{Subscription:name,State:state,IdentityType:user.type}" \
            --output table

      - name: Initialize Terraform
        run: terraform init -input=false

      - name: Validate Terraform
        run: terraform validate -no-color

      - name: Plan Terraform changes
        run: terraform plan -input=false -no-color -lock-timeout=60s -out=tfplan

      - name: Upload Terraform plan
        uses: actions/upload-artifact@v7
        with:
          name: terraform-plan
          path: infra/terraform/tfplan
          retention-days: 1
          if-no-files-found: error

  apply:
    name: Terraform Apply Gate
    needs: plan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: dev

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Log in to Azure using OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.15.8"

      - name: Download Terraform plan
        uses: actions/download-artifact@v8
        with:
          name: terraform-plan
          path: infra/terraform

      - name: Verify downloaded plan
        working-directory: infra/terraform
        run: test -f tfplan

      - name: Initialize Terraform
        working-directory: infra/terraform
        run: terraform init -input=false -no-color -lock-timeout=60s

      - name: Apply saved Terraform plan
        working-directory: infra/terraform
        run: terraform apply -input=false -no-color -lock-timeout=60s tfplan
