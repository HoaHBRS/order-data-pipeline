name: Terraform CD

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate:
    name: Verify dev deployment gate
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment:
      name: dev

    steps:
      - name: Confirm deployment approval
        run: echo "Dev deployment approved"
